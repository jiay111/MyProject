





























/*class CCylinder {
    private int x; // 下方圓圓心的x座標
    private int y; // 下方圓圓心的y座標
    private int z; // 下方圓圓心的z座標
    private int radius; // 下方圓的半徑
    private int height; // 圓柱體的高

    // (a) setLocation method
    public void setLocation(int x, int y, int z) {
        this.x = x;
        this.y = y;
        this.z = z;
    }

    // (b) setRadius method
    public void setRadius(int radius) {
        this.radius = radius;
    }

    // (c) setHeight method
    public void setHeight(int height) {
        this.height = height;
    }

    // (d) surfaceArea method
    public double surfaceArea() {
        return 2 * Math.PI * radius * (radius + height);
    }

    // (e) volume method
    public double volume() {
        return Math.PI * radius * radius * height;
    }

    // (f) showAll method
    public void showAll() {
        System.out.println("半徑: " + radius);
        System.out.println("高度: " + height);
        System.out.println("體積: " + volume());
    }
}

public class m4 {
    public static void main(String[] args) {
        CCylinder cylinder = new CCylinder();

        // (a) 設定圓心位置為(3, 4, 5)
        cylinder.setLocation(3, 4, 5);

        // (b) 設定半徑為1
        cylinder.setRadius(1);

        // (c) 設定高度為10
        cylinder.setHeight(10);

        // (d) 印出表面積
        System.out.println("表面積: " + cylinder.surfaceArea());

        // (e) 印出體積
        System.out.println("體積: " + cylinder.volume());

        // (f) 顯示半徑、高度及體積
        cylinder.showAll();
    }
}
*/